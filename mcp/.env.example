# Agentful MCP Server Configuration
# Copy this file to .env and customize as needed

# Project root directory (where .claude/ and .agentful/ are located)
AGENTFUL_PROJECT_ROOT=../

# MCP transport mode: stdio | http | sse
MCP_TRANSPORT=stdio

# Logging level: debug | info | warn | error
LOG_LEVEL=info

# HTTP/SSE transport settings (only used when MCP_TRANSPORT=http or sse)
MCP_PORT=3838
MCP_HOST=localhost

# Optional: CORS origin for HTTP transport
# MCP_CORS_ORIGIN=http://localhost:3000
