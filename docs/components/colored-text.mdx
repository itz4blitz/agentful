# Colored Text Components

export function Blue({ children }) {
  return <span style={{ color: '#19F' }}>{children}</span>
}

export function Orange({ children }) {
  return <span style={{ color: '#F90' }}>{children}</span>
}

export function Red({ children }) {
  return <span style={{ color: '#F00' }}>{children}</span>
}
